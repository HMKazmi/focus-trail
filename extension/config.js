/**
 * FocusTrail Extension – Configuration
 * Edit API_BASE_URL to point at your running server.
 */
const CONFIG = {
  API_BASE_URL: 'http://localhost:4000',
  
  // Logging configuration
  // Set LOG_LEVEL to 'debug' for detailed logs, 'info' for important logs only
  // 'warn' for warnings and errors only, 'error' for errors only
  LOG_LEVEL: 'info', // 'debug' | 'info' | 'warn' | 'error'
};
